/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  string,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializeGameInstructionAccounts = {
  game: PublicKey | Pda;
  playerEscrowTokenAccount: PublicKey | Pda;
  playerTokenAccount: PublicKey | Pda;
  mint: PublicKey | Pda;
  treasury: PublicKey | Pda;
  settings: PublicKey | Pda;
  player: Signer;
  tokenProgram?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type InitializeGameInstructionData = {
  gameId: string;
  amount: bigint;
  hash: Uint8Array;
};

export type InitializeGameInstructionDataArgs = {
  gameId: string;
  amount: number | bigint;
  hash: Uint8Array;
};

export function getInitializeGameInstructionDataSerializer(): Serializer<
  InitializeGameInstructionDataArgs,
  InitializeGameInstructionData
> {
  return struct<InitializeGameInstructionData>(
    [
      ['gameId', string()],
      ['amount', u64()],
      ['hash', bytes({ size: 32 })],
    ],
    { description: 'InitializeGameInstructionData' }
  ) as Serializer<
    InitializeGameInstructionDataArgs,
    InitializeGameInstructionData
  >;
}

// Args.
export type InitializeGameInstructionArgs = InitializeGameInstructionDataArgs;

// Instruction.
export function initializeGame(
  context: Pick<Context, 'programs'>,
  input: InitializeGameInstructionAccounts & InitializeGameInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'rockPaperScissors',
    'rock7uz5eZdz8fb1ZEfZ1aR428ncvkYBrgebwyzaqBG'
  );

  // Accounts.
  const resolvedAccounts = {
    game: { index: 0, isWritable: true as boolean, value: input.game ?? null },
    playerEscrowTokenAccount: {
      index: 1,
      isWritable: true as boolean,
      value: input.playerEscrowTokenAccount ?? null,
    },
    playerTokenAccount: {
      index: 2,
      isWritable: true as boolean,
      value: input.playerTokenAccount ?? null,
    },
    mint: { index: 3, isWritable: false as boolean, value: input.mint ?? null },
    treasury: {
      index: 4,
      isWritable: true as boolean,
      value: input.treasury ?? null,
    },
    settings: {
      index: 5,
      isWritable: false as boolean,
      value: input.settings ?? null,
    },
    player: {
      index: 6,
      isWritable: true as boolean,
      value: input.player ?? null,
    },
    tokenProgram: {
      index: 7,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    systemProgram: {
      index: 8,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeGameInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializeGameInstructionDataSerializer().serialize(
    resolvedArgs as InitializeGameInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
