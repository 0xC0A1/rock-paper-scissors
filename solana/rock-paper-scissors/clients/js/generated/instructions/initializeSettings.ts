/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  i64,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializeSettingsInstructionAccounts = {
  settings: PublicKey | Pda;
  signer: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type InitializeSettingsInstructionData = {
  timeForPenalization: bigint;
  timeForStale: bigint;
  playerFeeLamports: bigint;
};

export type InitializeSettingsInstructionDataArgs = {
  timeForPenalization: number | bigint;
  timeForStale: number | bigint;
  playerFeeLamports: number | bigint;
};

export function getInitializeSettingsInstructionDataSerializer(): Serializer<
  InitializeSettingsInstructionDataArgs,
  InitializeSettingsInstructionData
> {
  return struct<InitializeSettingsInstructionData>(
    [
      ['timeForPenalization', i64()],
      ['timeForStale', i64()],
      ['playerFeeLamports', u64()],
    ],
    { description: 'InitializeSettingsInstructionData' }
  ) as Serializer<
    InitializeSettingsInstructionDataArgs,
    InitializeSettingsInstructionData
  >;
}

// Args.
export type InitializeSettingsInstructionArgs =
  InitializeSettingsInstructionDataArgs;

// Instruction.
export function initializeSettings(
  context: Pick<Context, 'programs'>,
  input: InitializeSettingsInstructionAccounts &
    InitializeSettingsInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'rockPaperScissors',
    'rock7uz5eZdz8fb1ZEfZ1aR428ncvkYBrgebwyzaqBG'
  );

  // Accounts.
  const resolvedAccounts = {
    settings: {
      index: 0,
      isWritable: true as boolean,
      value: input.settings ?? null,
    },
    signer: {
      index: 1,
      isWritable: true as boolean,
      value: input.signer ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeSettingsInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializeSettingsInstructionDataSerializer().serialize(
    resolvedArgs as InitializeSettingsInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
